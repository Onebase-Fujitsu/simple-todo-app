spring:
  datasource:
    url: jdbc:h2:mem:todo_test_db
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    baseline-on-migrate: true
    url: jdbc:h2:mem:todo_test_db
    user: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true